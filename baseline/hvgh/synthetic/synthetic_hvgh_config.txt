# Strict HVGH baseline config for synthetic
# This config is launched by baseline/hvgh/_shard/launcher.py

repo_root = {REPO_ROOT}
runner = {SHARD_DIR}\hvgh_runner.py
out_dir = {CONFIG_DIR}\results_hvgh_synthetic

datasets = synthetic

# Original HVGH is notebook/Cython/TensorFlow based. This runner converts
# _shard/original/HVGH.ipynb to importable modules before training.
# Set eval_existing_only=1 only if HVGHlearn/segm000.txt files already exist.
eval_existing_only = 0
skip_completed = 0

# For a quick smoke test, uncomment:
# max_cases = 1

# Optional overrides. Leave blank to use original HVGH dataset-specific settings.
# epoch =
# iteration =
# win_size =
