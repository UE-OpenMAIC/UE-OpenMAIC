
from __future__ import annotations

from pathlib import Path

ROOT = Path(r"D:\code\teacherT2S")
TARGET = ROOT / "baseline" / "ticc" / "_shard" / "ticc_preprocessing.py"
BACKUP = TARGET.with_suffix(TARGET.suffix + ".bak_before_default_data_root")

FUNC =
