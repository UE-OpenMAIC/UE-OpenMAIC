# Strict TICC baseline config for pamap2_zero
# Put this file at:
#   D:\code\teacherT2S\baseline\ticc\pamap2_zero\pamap2_zero_ticc_config.txt
#
# This config is launched by:
#   D:\code\teacherT2S\baseline\ticc\_shard\launcher.py
#
# Zero-version protocol:
#   datasets = pamap2_zero
#   use all sensor columns after timestamp/activity_id
#   remove activity_id=0
#
# Original PAMAP2 TICC algorithm setting in experiments.py:
#   TICC(window_size=3, maxIters=3, num_proc=1)
#
# Best-param comment in original experiments.py:
#   exp_on_PAMAP2(3000, 1e-3, 1e-3)

repo_root = {REPO_ROOT}
runner = {SHARD_DIR}\ticc_runner.py

# Use a new output directory to avoid mixing with previous window_size=0 / max_iters=0 / beta=2200 results.
out_dir = {CONFIG_DIR}\results_ticc_pamap2_zero_w3_iter3_b3000_thr1e3

data_root = {REPO_ROOT}\Time2State\data

# Important: keep pamap2_zero, not pamap2.
# The _shard loader should route this to the zero-version loader.
datasets = pamap2_zero

# Fixed: original TICC PAMAP2 algorithm setting.
window_size = 3
max_iters = 3

# Original exp_on_PAMAP2 uses num_proc=1.
num_proc = 1

# Best-param comment in original experiments.py:
# exp_on_PAMAP2(3000, 1e-3, 1e-3)
beta = 3000
lambda_parameter = 0.001
threshold = 0.001

seed = 102
cp_margin_ratio = 0.01

# Important:
# Use 0 for corrected rerun, otherwise existing wrong results may be skipped.
skip_completed = 0

# For a quick test, uncomment:
# max_cases = 1
