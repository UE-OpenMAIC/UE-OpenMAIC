# Strict TICC baseline config for mocap
# Put this file at:
#   D:\code\teacherT2S\baseline\ticc\mocap\mocap_ticc_config.txt
#
# This config is launched by:
#   D:\code\teacherT2S\baseline\ticc\_shard\launcher.py
#
# Strict original TICC MoCap baseline setting:
#   window_size = 5
#   maxIters    = 10
#   beta        = 2200
#   lambda      = 0.001
#   threshold   = 0.0001

repo_root = {REPO_ROOT}
runner = {SHARD_DIR}\ticc_runner.py

# Use a new output directory to avoid mixing with the previous window_size=0 / max_iters=0 run.
out_dir = {CONFIG_DIR}\results_ticc_mocap_w5_iter10

data_root = {REPO_ROOT}\Time2State\data

datasets = mocap

# Fixed: do NOT use 0 here.
window_size = 5
max_iters = 10

# Safer on Windows.
# Original script often used larger num_proc, but 1 is more stable for debugging.
num_proc = 1

beta = 2200
lambda_parameter = 0.001
threshold = 0.0001
seed = 102
cp_margin_ratio = 0.01

# Important:
# Use 0 for this corrected rerun, otherwise existing wrong results may be skipped.
skip_completed = 0

# For a quick test, uncomment:
# max_cases = 1
